using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class EnermyHP : MonoBehaviour
{
    /*private Enermy enermy;
    public Slider hpvar;
    private float maxHp;
    private float curHp;
    private bool isDie = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        transform.position = enermy.position+new Vector3(0,-1f,0) ;
        hpvar.value = curHp / maxHp;
    }*/
}
 